package org.ob.wso2.ca;

/**
 * Create CA's private and public keys
 */
public class generateCAKeys {
// openssl req -x509 -newkey rsa:2048 -keyout caKey.pem -out cert.pem -days 365
// openssl x509 -outform der -in cert.pem -out ca.cer
// keytool -import -alias selfCA -file ca.cer -keystore client-truststore.jks -storepass wso2carbon



}
